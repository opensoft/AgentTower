include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # Treat invalid_assignment as error to catch dynamic-typing slip-ups.
    invalid_assignment: error
    # Generated files are auto-managed by build_runner.
    missing_required_param: error
    missing_return: error
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/generated_plugin_registrant.dart"
    - "build/**"
    - ".dart_tool/**"
    - ".fvm/**"

linter:
  rules:
    # Enabled in addition to flutter_lints defaults.
    always_declare_return_types: true
    avoid_print: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_locals: true
    require_trailing_commas: true
    sort_pub_dependencies: false  # pubspec ordering is grouped by purpose
    unawaited_futures: true
    use_super_parameters: true
